// src/hooks/useSound.js

// Hook neutralisé pour la Mission 0 (Silence Radio)
export const useSound = () => {
  const playClickSound = () => {
    // Silence
  };

  const playHoverSound = () => {
    // Silence
  };

  return { playClickSound, playHoverSound };
};